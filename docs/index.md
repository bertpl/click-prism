# click-prism

See your Click CLI from every angle.

Tree (plain or [Rich](https://rich.readthedocs.io/)-styled) and JSON
output for [Click](https://click.palletsprojects.com/) command hierarchies.

The package is pre-implementation — proper documentation lands in later
phases. For now this stub exists so Read the Docs has something to
build.
